name: Deploy to Prod

on:
  push:
    branches: [main]

jobs:
  deploy_prod:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy to Production
        run: echo "🚀 Production deployment läuft (simuliert)"
